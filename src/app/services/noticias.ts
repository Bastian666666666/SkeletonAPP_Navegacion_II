export class Noticias {
    //Agregue las variables que se van a utilizar
    //Agregue el signo de exclamacion para que no de error al pedir que lo inicialice
    id!: number;
    titulo!: string;
    texto!: string;
}